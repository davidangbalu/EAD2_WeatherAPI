<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:WeatherAPIClient.ViewModels"
             x:Class="WeatherAPIClient.MainPage">

    <ContentPage.BindingContext>
        <viewModels:MainViewModel/>
    </ContentPage.BindingContext>

    <ListView HasUnevenRows="True"
              ItemsSource="{Binding WeatherInformation}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Label Text="{Binding Id}"/>
                        <Label Text="{Binding City}"/>
                        <Label Text="{Binding Conditions}"/>
                        <Label Text="{Binding MaxTemperature}"/>
                        <Label Text="{Binding MinTemperature}"/>
                        <Label Text="{Binding WindDirection}"/>
                        <Label Text="{Binding WindSpeed}"/>
                        <Label Text="{Binding Outlook}"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
</ListView>

</ContentPage>
